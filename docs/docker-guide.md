# Docker Guide
## Quick Start
1. Install Docker
2. Run `docker-compose up`
## Environment Variables Reference
* `GPU_IMAGE_PROCESSING_PORT`: The port to use for the API
## Production Deployment Checklist
1. Configure environment variables
2. Run `docker-compose up -d`
